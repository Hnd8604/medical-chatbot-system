FHIR_BASE_URL=http://localhost:8080/fhir
FHIR_REQUEST_TIMEOUT_SECONDS=20
APP_NAME=Dịch vụ Chatbot Y tế

LLM_PROVIDER=openai
MODEL_SIMPLE=gpt-4o-mini
MODEL_COMPLEX=gpt-4.1-mini
LLM_REQUEST_TIMEOUT_SECONDS=20
ENABLE_LLM_ANSWER=true

# LLM Router (M16): model rẻ phân loại SIMPLE/COMPLEX thay cho keyword thuần.
# Tắt (false) → chỉ dùng keyword classifier như trước.
ENABLE_LLM_ROUTER=false
MODEL_ROUTER=gpt-4o-mini

# AI Gateway (LiteLLM). Đường LLM duy nhất; bật khi có LITELLM_MASTER_KEY.
LITELLM_BASE_URL=http://localhost:4000
LITELLM_MASTER_KEY=sk-local-dev

# Rolling summary hội thoại (qua LiteLLM gateway).
# Chỉ gọi LLM khi tổng số message của session >= SUMMARY_TRIGGER_MESSAGE_COUNT.
ENABLE_LLM_SUMMARY=true
MODEL_SUMMARY=gpt-4o-mini
SUMMARY_TRIGGER_MESSAGE_COUNT=8

# Terminology enrichment: giải thích mã y khoa (chỉ chạy khi câu hỏi mang ý "giải thích").
# RxNorm + MedlinePlus là API công khai (không cần key). LOINC cần tài khoản loinc.org
# miễn phí; thiếu LOINC_USERNAME/PASSWORD -> nhánh LOINC tự bỏ qua, 2 nguồn kia vẫn chạy.
LOINC_ENABLED=true
RXNORM_ENABLED=true
MEDLINEPLUS_ENABLED=true
LOINC_USERNAME=
LOINC_PASSWORD=
TERMINOLOGY_TIMEOUT_SECONDS=5

# ---- LangGraph agent (M-LG) ----
# Endpoint POST /chat/langgraph: router -> planner -> validator -> executor -> answer.
# Tắt mặc định; /chat vẫn là đường chính cho tới khi bộ eval (M-LG6) cho số liệu.
ENABLE_LANGGRAPH_AGENT=false

# Model theo stage. Bỏ trống -> rơi về MODEL_ROUTER / MODEL_COMPLEX / MODEL_SIMPLE.
MODEL_AGENT_ROUTE=
MODEL_AGENT_PLANNER=
MODEL_AGENT_CHAT=

# Số quyết định route được nhớ trong LRU của tiến trình (0 = tắt).
AGENT_ROUTE_CACHE_SIZE=512
# Trần số step của một plan sau khi validate.
AGENT_MAX_PLAN_STEPS=4
# Evidence budget: trần ký tự evidence đưa vào prompt answer (~4 ký tự/token).
AGENT_EVIDENCE_MAX_CHARS=12000
# Fast-path: plan 1 bước + data_only + ít evidence -> dùng câu template, không gọi answer LLM.
AGENT_TEMPLATE_FAST_PATH=false
AGENT_TEMPLATE_FAST_PATH_MAX_EVIDENCE=5
# Quota vượt ngưỡng -> bỏ router/planner LLM, dùng rule-based extractor.
AGENT_LOW_COST_MODE_RATIO=0.9

# Plan cache: cache "ý định đã validate" (không chứa PHI), dùng chung mọi user, TTL dài.
ENABLE_PLAN_CACHE=true
PLAN_CACHE_COLLECTION_NAME=medical_plan_cache
PLAN_CACHE_TTL_SECONDS=86400
PLAN_CACHE_SIMILARITY_THRESHOLD=0.95

# QUYẾT ĐỊNH BẢO MẬT ĐANG CHỜ CHỐT (docs/M-langgraph-agent.md §2.3d).
# false = giữ chính sách /chat hiện tại (USER không được get_resource_by_id).
# true  = cho phép, executor kiểm tra chủ sở hữu resource sau khi fetch.
AGENT_ALLOW_USER_RESOURCE_LOOKUP=false
